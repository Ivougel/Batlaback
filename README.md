# Batlaback

Клон Backpack Battles (браузер). Открыть `index.html`.

## Предметы

Правки в **`tools/items-migrated.json`**, затем:

```bash
bash tools/build-catalog.sh
```

Подробно: `tools/CATALOG.md`
